{
  "name": "riddle-tablets",
  "version": "1.0.0",
  "private": true,
  "description": "A password-authored gallery of animated riddle tablets",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@upstash/redis": "^1.38.1",
    "dotenv": "^16.4.5",
    "express": "^4.22.2"
  },
  "devDependencies": {
    "playwright-core": "^1.62.1"
  }
}
